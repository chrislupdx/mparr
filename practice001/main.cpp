#include "arr.h"

int main()
{
    table object; 
    object.build(); //creates an array of LLL
    object.display(); //displays the array
    
    object.removeArr(); 
    //PLEASE PUT YOUR CODE HERE to call the function assigned
    //cout << "matching firsts of EACH array " << object.firstCount() << endl;
    //object.removeLast(); //wrapper

    object.display();
    
    
    return 0;
}
